# PDF Image Tools 利用しやすさ向上の機能追加案

作成日: 2026-07-13
対象: `pdf-image-tools` の現行実装

> **実装状況（2026-07-13）:** 本文のP0〜P2を実装済みです。詳細は各項目とREADMEを参照してください。今後の改善候補として、実利用データに基づく上限値調整とブラウザ横断E2Eテストを継続します。

## 実装完了サマリー

- [x] Undo / Redo、操作履歴、未保存警告
- [x] IndexedDBのローカル下書き、自動保存、再開・破棄
- [x] ファイル単位の検証、部分成功、再試行、キャンセル
- [x] PDF・画像の出力名、用紙、余白、画質、形式、ZIP設定
- [x] 完全削除の保存停止、残存一覧、明示的なカバー保存
- [x] ページ回転、複製、空白挿入、複数選択、任意抽出
- [x] 画像の自然順、重複除外、回転、白黒、コントラスト、トリミング
- [x] 遅延サムネイル、処理量表示、キャンセル
- [x] キーボード、タッチ、ARIA、日本語文書指定
- [x] 目的別ガイド、画面内ヘルプ、正しい問い合わせ導線
- [x] ツール間のPDF直接受け渡し
- [x] PWA、オフライン起動、更新通知
- [x] レシピ、ブラウザ内バッチ、CLI

## 目的

現在の「PDF編集」「PDF結合」「画像からPDFへの変換」を、初めての利用者でも迷いにくく、失敗しても復旧でき、大きなファイルでも安心して使えるツールにするための機能追加案です。

本ツールの強みは、すべての処理がブラウザ内で完結し、ファイルをサーバーへ送信しない点です。この特徴を維持したまま、次の3点を優先します。

1. 誤操作や再読み込みで作業を失わない
2. 入力・出力条件とエラーの内容を利用者が理解できる
3. マウス、キーボード、タッチ操作のいずれでも使いやすくする

## 優先度の定義

- **P0**: 利用時の事故、保存失敗、情報漏えいリスクを減らすため、最初に対応したい
- **P1**: 日常的な作業効率や利用者層を大きく改善する
- **P2**: 利用場面を広げる発展機能
- 規模の **S / M / L** は相対的な実装量の目安

## 提案一覧

| 優先度 | 機能 | 主な効果 | 規模 |
| --- | --- | --- | --- |
| P0 | Undo / Redo・未保存警告 | 誤削除や設定変更からすぐ復旧できる | M |
| P0 | ローカル下書きの自動保存・再開 | 再読み込みやタブ移動で作業を失わない | M |
| P0 | ファイル単位の検証・エラー回復 | 一部の読込失敗で全作業が止まらない | M |
| P0 | 出力設定・出力完了パネル | 用途に合うPDF・画像を迷わず保存できる | M |
| P0 | 完全削除（redact）の安全な保存フロー | 削除に失敗した文字データの見落としを防ぐ | S〜M |
| P1 | ページ編集・PDF結合のページ単位操作 | 抽出、回転、結合を1画面で完結できる | L |
| P1 | 画像の一括補正・自動整列 | スキャン画像や連番画像を素早くPDF化できる | M |
| P1 | 大容量ファイル向け軽量モード | メモリ不足や長時間の無反応を減らす | L |
| P1 | キーボード・タッチ・アクセシビリティ対応 | 操作手段や端末に依存せず利用できる | M |
| P1 | 目的別スタート画面・操作ガイド | 初めてでも目的の機能へ到達しやすい | S〜M |
| P1 | ツール間の直接受け渡し | ダウンロードと再選択の手間をなくす | M |
| P2 | オフライン利用・PWA | 通信できない環境でも起動できる | M |
| P2 | バッチ処理・作業レシピ | 同じ処理を多数のファイルへ再利用できる | L |

## P0: 最初に追加したい機能

### 1. Undo / Redo・操作履歴・未保存警告

**現状の課題**

- ページ削除や並び替えは即時反映され、ページ編集全体をリセットする以外に戻す方法がない
- コンテンツ、テキストボックス、ヘッダー、ページ番号を横断する履歴がない
- ファイル変更、タブ移動、ページ再読み込みの前に未保存の変更を知らせる仕組みがない

**追加内容**

- ページ削除・並び替え、コンテンツ編集、テキストボックス、ヘッダー、ページ番号を共通履歴で管理
- 「元に戻す」「やり直す」ボタンと `Ctrl/Cmd + Z`、`Ctrl/Cmd + Shift + Z` を追加
- 直近20〜50操作の履歴一覧と、操作名（例: 「3ページ目を削除」）を表示
- 未保存状態を画面上に表示し、ファイル変更・タブ移動・再読み込み時に確認する

**受け入れ条件**

- 主要な編集操作を少なくとも20手前まで戻せる
- 入力欄への文字入力中は、ブラウザ標準のUndoと編集履歴のショートカットが競合しない
- PDFを保存した時点が履歴上で分かる

### 2. ローカル下書きの自動保存・再開

**現状の課題**

編集状態はReactの画面状態だけにあり、再読み込みすると失われます。また、ツールのタブを切り替えるとコンポーネントが外れるため、途中の作業が消える可能性があります。

**追加内容**

- IndexedDBへ元ファイル、編集設定、ページ順、操作履歴をローカル保存
- 起動時に「前回の作業を再開」「破棄」を表示
- 「自動保存: オン/オフ」「下書きを削除」「保存日時」を設定画面に追加
- 容量上限と自動削除期限を表示し、古い下書きを管理できるようにする
- 保存データが端末外へ送信されないことを明記

**受け入れ条件**

- ページ再読み込み後に、同じページ・ズーム・編集内容から再開できる
- 保存容量不足では処理を止めず、自動保存だけが失敗したことを画面に通知する
- 共有端末向けに自動保存を無効化できる

### 3. ファイル単位の検証・エラー回復

**現状の課題**

- 複数ファイルの読込に `Promise.all` を使う箇所があり、1件の失敗で有効なファイルも追加されない
- PDF読込やサムネイル生成に失敗した場合、読み込み表示が残り続ける可能性がある
- 非対応、破損、パスワード保護、メモリ不足などの原因が同じ一般的なエラーとして表示される
- ドロップした対象外ファイルは理由を示さず除外される

**追加内容**

- ファイルごとに「待機中 / 読込中 / 完了 / 警告 / 失敗」を表示する読込キュー
- `Promise.allSettled` 相当の部分成功にし、成功分だけ処理を継続可能にする
- パスワード付きPDFにはパスワード入力と再試行を表示
- 破損、非対応形式、重複、容量超過を区別し、対処方法を添える
- 「再試行」「このファイルを除外」「すべてキャンセル」を追加

**受け入れ条件**

- 10件中1件が失敗しても、残り9件を結合・変換できる
- エラー後に読み込み中表示が残らない
- エラー文だけで次に行う操作が分かる

### 4. 出力設定・出力完了パネル

**現状の課題**

- 画像からPDFへの変換は画像サイズをそのままページサイズにし、配置や余白を選べない
- 入力画像を一律JPEGとして追加するため、透明PNGや画質を重視する用途を選べない
- 結合結果は `merged.pdf`、画像変換結果は `images_to_pdf.pdf` など固定名で保存される
- PNG出力は固定倍率で全ページを生成し、「すべて保存」は複数ファイルの連続ダウンロードになる

**追加内容**

- PDF: 出力名、A4/A3/Letter/画像原寸、縦横、余白、`contain`/`cover`、背景色を選択
- 画像: PNG/JPEG/WebP、ページ範囲、DPIまたは倍率、画質を選択
- 「高画質」「標準」「ファイルサイズ優先」のプリセットを用意
- 複数画像はZIP 1ファイルで保存
- 完了後にファイル名、ページ数、容量、使用設定と「続けて編集」を表示

**受け入れ条件**

- 出力前にページサイズと画像の収まり方をプレビューできる
- 設定値が次回もローカルに保持される
- 複数画像の保存時にブラウザの連続ダウンロード許可を要求しない

### 5. 完全削除（redact）の安全な保存フロー

**現状の課題**

文字データの完全削除には事後検証がありますが、削除を保証できなかった対象があっても警告後にカバーのみのPDFを保存できる流れです。利用者が警告を見落とすと、見た目では隠れていても文字データが残る可能性があります。

**追加内容**

- 完全削除の検証に失敗した場合は、既定で保存を停止
- 失敗した文字列、ページ、理由を一覧表示
- 「編集へ戻る」と、リスク説明を確認した上での「カバーのみで保存」を明確に分離
- 保存成功時に「検証済み」バッジと検証結果レポートを表示・出力
- 単色でない背景や曲線パスなど、既知の制約を操作前に短く案内

**受け入れ条件**

- 検証失敗を一般的な `alert` だけで処理しない
- 利用者が明示的に選択しない限り、文字データが残ったファイルをダウンロードしない
- 保存後にも完全削除の成否を確認できる

## P1: 作業効率と利用範囲を広げる機能

### 6. ページ編集・PDF結合のページ単位操作

- ページの90度回転、複製、空白ページ挿入を追加
- `Shift` で範囲選択、`Ctrl/Cmd` で個別選択、奇数・偶数・範囲指定を追加
- 連続範囲だけでなく、選択した任意ページを抽出可能にする
- PDF結合画面で各PDFをページ単位に展開し、不要ページの除外とファイルをまたいだ並び替えを可能にする
- ページの右クリックまたは「…」メニューにも同じ操作を用意し、ドラッグ操作だけに依存しない

### 7. 画像の一括補正・自動整列

- ファイル名の自然順、撮影日時、更新日時による並び替え
- 重複画像の検出と除外
- 90度回転、トリミング、傾き補正、余白除去を画像ごとまたは一括で適用
- スキャン文書向けに白黒化、コントラスト調整、用紙サイズ統一を追加
- 個別設定と一括設定の適用範囲を常に表示

### 8. 大容量ファイル向け軽量モード

**現状の課題**

PDF編集では全ページのサムネイルを順番にCanvas描画し、Data URLとして保持します。多数ページのPDFでは待ち時間とメモリ消費が大きくなります。

**追加内容**

- 表示範囲付近だけサムネイルを作る遅延生成とリスト仮想化
- 読込前にファイル容量、ページ数、画像の総画素数から処理量を概算
- 「通常」「低メモリ」のモード選択
- 処理件数、残り件数、経過時間とキャンセルボタンを表示
- 生成済みObject URLやCanvasを不要になった時点で解放

### 9. キーボード・タッチ・アクセシビリティ対応

- ドロップゾーンをボタンとして扱い、Enter/Spaceでファイル選択を開く
- タブへ `role="tab"`、`aria-selected`、関連パネルの指定を追加
- 進捗を `role="progressbar"`、エラーと完了通知を `aria-live` で通知
- アイコンだけのボタンへ読み上げ可能な名前を追加
- ページやファイルをキーボードで上下移動できるようにする
- タッチ端末ではドラッグ用ハンドルを拡大し、「上へ」「下へ」操作も常に表示
- `index.html` の言語指定を日本語にし、フォーカス表示と色コントラストを確認

### 10. 目的別スタート画面・操作ガイド

- 初回起動時に「ページを並べ替える」「PDFを結合する」「画像をPDFにする」「文字を編集・削除する」などの目的別カードを表示
- 複雑なPDF編集は、ファイル選択 → 編集方法 → 保存の3ステップで案内
- 初回だけ表示する吹き出し、用語説明、サンプルファイルを用意
- 対応形式、推奨上限、パスワードPDF、背景付き要素の編集制約を画面内ヘルプに掲載
- ヘッダーのGitHubリンクを実際のリポジトリまたは問い合わせ先へ変更

### 11. ツール間の直接受け渡し

- PDF結合後に「続けてPDF編集」を表示し、Blobを画面内で引き渡す
- 画像からPDFへの変換後も「ページ番号を追加」「さらにPDFを結合」を選べるようにする
- 編集結果をPNGプレビューへ渡し、保存前に確認できるようにする
- 引き渡したデータのファイル名と設定を保持し、戻る操作にも対応する

## P2: 発展機能

### 12. オフライン利用・PWA

- アプリを端末へインストール可能にする
- 初回読込後は、ネットワークがなくても本体と同梱フォントを利用可能にする
- 新版がある場合は作業中の更新を避け、更新内容と適用ボタンを表示
- キャッシュとローカル下書きを別々に削除できるようにする

### 13. バッチ処理・作業レシピ

- 同じページ番号、ヘッダー、出力設定などを「レシピ」としてJSON保存
- 複数ファイルへ同じレシピをブラウザ内で順番に適用
- `merge`、`extract`、`reorder`、`images-to-pdf` をCLIから実行可能にする
- GUIで作ったレシピをCLIでも使える形式にし、定型作業を再現可能にする

## 推奨ロードマップ

### フェーズ1: 安全性と失敗回復

1. PDF読込の例外処理と、ファイル単位のエラー表示
2. 完全削除に失敗した場合の保存停止
3. Undo / Redo、未保存表示、破棄確認
4. 出力ファイル名、PNGのZIP保存、完了通知

### フェーズ2: 日常作業の効率化

1. ローカル下書きと再開
2. PDF・画像の出力設定とプリセット
3. ページ回転、複製、複数選択、任意ページ抽出
4. PDF結合のページ単位編集とツール間受け渡し

### フェーズ3: 利用範囲の拡大

1. 遅延サムネイル、仮想化、キャンセル可能な処理
2. キーボード・タッチ・スクリーンリーダー対応
3. 目的別ガイドと画面内ヘルプ
4. PWA、バッチ処理、作業レシピ

## 最初のリリースで達成したい指標

- 読込失敗後、ページを再読み込みせずに別ファイルで再試行できる
- 編集操作の100%をUndoできる
- 未保存の変更を意図せず失う経路がない
- 完全削除の検証失敗ファイルが、確認なしで保存されない
- 出力名、ページサイズまたは解像度、画質を利用者が選択できる
- マウスなしで、ファイル選択・主要編集・保存まで操作できる

## 現行実装から確認できた主な根拠

- `src/App.tsx`: 3ツールは独立したタブとして切り替えられ、初期表示は最も多機能なPDF編集
- `src/components/PdfEditor.tsx`: 編集状態は画面内に保持され、保存・PNG出力・範囲抽出を提供
- `src/hooks/usePdfDocument.ts`: PDF読込と全ページのサムネイル生成を実行
- `src/components/PdfMerger.tsx`: PDF単位の並び替えと全ページ結合を提供し、出力名は固定
- `src/utils/imagesToPdf.ts`: 画像ごとの原寸ページを作成し、画像をJPEGとして配置
- `src/components/Dropzone.tsx` と `src/components/pdfEdit/PageManagementPanel.tsx`: 主にクリックとドラッグ操作で構成
- `src/utils/redactionVerification.ts`: 完全削除後の文字データ残存検証を提供
- `index.html`: 現在の文書言語は英語指定

## 補足

実装時は、現在の「ファイルをサーバーへ送信しない」という方針を変更せず、下書き、設定、診断情報も既定では端末内だけに保存することを推奨します。
